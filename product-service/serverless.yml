service:
  name: product-service
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name
frameworkVersion: "2"

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack
  - serverless-offline

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: eu-west-1

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules:
      forceExclude:
        - aws-sdk

functions:
  getProductsList:
    handler: src/getProductsList.get
    events:
      - http:
          method: get
          path: products
  getProductsById:
    handler: src/getProductsById.get
    events:
      - http:
          method: get
          path: products/{productId}
